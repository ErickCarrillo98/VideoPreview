//we may need to make use of the .play() and .pause() methods - reference

//We may also need the video to be muted as modern browsers want users to interact with (click) videos for them to start playing.

function play() {
    var x= document.getElementsByClassName("my-video");
    my-video.play();
}

function pause() {
    my-video.pause();
}

